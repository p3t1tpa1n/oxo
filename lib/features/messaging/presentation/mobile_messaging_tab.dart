// ============================================================================
// MOBILE MESSAGING TAB - OXO TIME SHEETS
// Tab Messages avec la messagerie
// Utilise STRICTEMENT AppTheme
// ============================================================================

import 'package:flutter/material.dart';
import '../../../pages/messaging/ios_messaging_page.dart';

class MobileMessagingTab extends StatelessWidget {
  const MobileMessagingTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const IOSMessagingPage();
  }
}

